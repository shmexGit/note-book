{
  "name": "fastify-server",
  "version": "0.0.1",
  "main": "dist/src/index",
  "types": "dist/src/index",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node dist/src/index.js",
    "build": "tsc",
    "lint": "eslint src/*.ts",
    "clean": "rm -r dist/*"
  }
}
